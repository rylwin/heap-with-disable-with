== README

Start rails application, then visit the following pages to test using
disable_with in a form both with and without the Heap tracker:

* http://localhost:3000/submit_without_heaps/new
* http://localhost:3000/submit_with_heaps/new

Note the different values displayed after submitting the form.
